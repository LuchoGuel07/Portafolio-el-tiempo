package co.com.eltiempo.certificacion.metro.interactions;

import org.openqa.selenium.JavascriptExecutor;

public class CerrarAnuncios {
//    public void cerrarPublicidad() {
//        JavascriptExecutor executor = (JavascriptExecutor) this.getDriver();
//        if (irATiempoPublicidad.isCurrentlyVisible()) { executor.executeScript(argumentsClick, irATiempoPublicidad); }
//        if (btnNoAceptarNotificaciones.isCurrentlyVisible()) {  executor.executeScript(argumentsClick, btnNoAceptarNotificaciones); }
//        if (irATiempoPublicidad.isCurrentlyVisible()) { executor.executeScript(argumentsClick, irATiempoPublicidad); }
//        if (imagenPublicidadUno.isCurrentlyVisible()) { executor.executeScript(argumentsClick, imagenPublicidadUno); }
//    }
}
